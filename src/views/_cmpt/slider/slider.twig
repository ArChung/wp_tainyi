<div class="slider gee " data-gene="init:slider">
	{% for item in banners %}

		<div class="{{ (ratio_cls)? ratio_cls : "neck-ratio" }}  slick-slide-container" >
			<div class="inset-0 coverBg slick-slide-bg" style="background-image:url('{{item.src}}')">{# {% if item.title %}
							<div class="container h-full flex items-center px-4 justify-center">
								<h1 class='text-3xl font-bold tracking-widest text-primary'>{{item.title}}</h1>
							</div>
						{% endif %} #}
			</div>


		</div>
	{% endfor %}
</div>
